<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal"
    android:background="@color/backgroundLight">

    <!-- Titel -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📥 Fortschritt importieren"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/textDark"
        android:layout_marginBottom="16dp" />

    <!-- Datei auswählen -->
    <Button
        android:id="@+id/selectButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Datei auswählen"
        android:textSize="16sp"
        android:background="@drawable/button_primary"
        android:textColor="@color/white"
        android:layout_marginBottom="16dp" />

    <!-- Vorschau-Liste -->
    <ListView
        android:id="@+id/previewList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@color/divider_light"
        android:dividerHeight="1dp"
        android:background="@color/white"
        android:layout_marginBottom="16dp" />

    <!-- Checkbox für Überschreiben -->
    <CheckBox
        android:id="@+id/overwriteCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vorhandene Einträge überschreiben"
        android:textSize="14sp"
        android:textColor="@color/textDark"
        android:layout_marginBottom="16dp" />

    <!-- Import bestätigen -->
    <Button
        android:id="@+id/confirmButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Import bestätigen"
        android:textSize="16sp"
        android:background="@drawable/button_secondary"
        android:textColor="@color/white"
        android:layout_marginBottom="24dp" />

</LinearLayout>